// @flow
// import React from 'react';
// import { useFieldsetState } from './index';

// $FlowFixMe
describe('Shared Fieldset tests', () => {
  // $FlowFixMe
  it('initialization success', () => {
    // type InitialState = {
    //   field: { value: string, tmpParam: boolean },
    //   meta: { tmpParam: boolean }
    // };

    // const initialState = {
    //   field: { value: 'value', tmpParam: true },
    //   meta: { tmpParam: false },
    // };

    // const [
    //   state,
    //   setState,
    //   validate,
    //   isValidationFailed,
    // ] = useFieldsetState<InitialState>(initialState);

    // console.log(state, setState, validate, isValidationFailed);
    // console.log(state.meta.tmpParam);
  });
});
